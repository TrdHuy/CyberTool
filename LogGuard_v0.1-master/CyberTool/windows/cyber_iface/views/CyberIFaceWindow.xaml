<cyw:CyberWindow x:Class="cyber_tool.windows.cyber_iface.views.CyberIFaceWindow"
				 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				 xmlns:cyw="clr-namespace:cyber_tool.app_resources.controls.cyber_window"
				 xmlns:vm="clr-namespace:cyber_tool.windows.cyber_iface.view_models"
				 xmlns:local="clr-namespace:cyber_tool.windows.cyber_iface.views"
				 mc:Ignorable="d"
				 Title="CyberTool"
				 Height="820"
				 Width="1066"
				 Background="{StaticResource Background_Level1}"
				 Style="{StaticResource DefaultCyberStyle}"
				 FontFamily="Calibri">
	<Window.DataContext>
		<vm:CyberIFaceWindowViewModel />
	</Window.DataContext>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1.71*" />
			<ColumnDefinition Width="8.29*" />
		</Grid.ColumnDefinitions>

		<Grid Grid.RowSpan="3"
			  Margin="0,0,10,0"
			  Background="{StaticResource Background_Level2}">
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="2*" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<ListView BorderThickness="0"
					  Grid.Row="1"
					  ItemsSource="{Binding PageHeaderItems}"
					  SelectedItem="{Binding SelectedHeaderItem}"
					  Background="{StaticResource Background_Level2}">
				<ListView.ItemContainerStyle>
					<Style TargetType="ListViewItem">
						<Setter Property="Template">
							<Setter.Value>
								<ControlTemplate TargetType="ListViewItem">
									<Grid Height="40">
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*" />
											<ColumnDefinition Width="2*" />
										</Grid.ColumnDefinitions>
										<Rectangle x:Name="FeedbackRec"
												   StrokeThickness="1"
												   Fill="#19B8E7F9"
												   Stroke="#FFB8EAF9"
												   Visibility="Hidden"
												   Grid.ColumnSpan="2" />
										<Path Data="{Binding HeaderGeometryData,Converter={StaticResource GeoPathConverter}}"
											  Fill="{StaticResource Foreground_Level3}"
											  Height="20"
											  Width="20"
											  Stretch="Uniform" />
										<Label Grid.Column="1"
											   VerticalAlignment="Center"
											   Foreground="{StaticResource Foreground_Level3}"
											   Content="{Binding Header}">
										</Label>

									</Grid>
									<ControlTemplate.Triggers>
										<Trigger Property="IsMouseOver"
												 Value="True">
											<Setter Property="Visibility"
													TargetName="FeedbackRec"
													Value="Visible" />
										</Trigger>

										<Trigger Property="IsSelected"
												 Value="True">
											<Setter Property="Visibility"
													TargetName="FeedbackRec"
													Value="Visible" />
											<Setter Property="Fill"
													TargetName="FeedbackRec"
													Value="{StaticResource SelectedItemBackground}" />
											<Setter Property="StrokeThickness"
													TargetName="FeedbackRec"
													Value="0" />
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Setter.Value>
						</Setter>
						<Style.Triggers>
							<DataTrigger Binding="{Binding Header}"
										 Value="{x:Null}">
								<Setter Property="Template">
									<Setter.Value>
										<ControlTemplate TargetType="ListViewItem">
										</ControlTemplate>
									</Setter.Value>
								</Setter>
							</DataTrigger>


						</Style.Triggers>


					</Style>

				</ListView.ItemContainerStyle>

			</ListView>
		</Grid>

		<ContentPresenter Grid.Column="1"
						  Grid.RowSpan="3"
						  Margin="0,20,10,0"
						  Content="{Binding ServiceContent}">
			
		</ContentPresenter>
	</Grid>
</cyw:CyberWindow>
